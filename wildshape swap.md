Replace a known form (you may swap one form on a Long Rest). Forgets the old form; if you name a
replacement it starts a scan for it (finish with `!wildshape learn`).

__Usage__
`!wildshape swap Wolf for "Giant Badger"`
`!wildshape swap Wolf` forgets the form.
