Shows your Wild Shape status and the command list.

`!wildshape` shows your current form, uses, level, subclass, and known forms.
`!wildshape <form>` transforms into a known form.

Start with `!wildshape setup`, then `!wildshape scan` and `!wildshape learn` to add forms.
